Prerequisities:
* dmd v2.095 compiler
* cmake
* cmaked2

Building:
Inside the source dir:
# mkdir build && cd build && cmake .. && make

After completing above steps:
# ls build/doc
# ls build/bin
# ls build/inc
# ls build/lib
